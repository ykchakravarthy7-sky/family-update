# Approved final reveal

The final screen uses the approved artwork at `assets/final-invitation.png` to preserve the exact visual details of the reference. The replay button is a real HTML button over the printed button. Its accessible name is Replay surprise. The original Family OS sequence and timing are unchanged.

The original design is preserved at commit `1fff5e3f7f57b934b6d71ff14a69a39f1f3b3e49`. Restore its `index.html` to roll back the final screen. The image-based layout is intended for the approved wording and 10% status; future text changes require an editable HTML layout or a revised approved artwork.
